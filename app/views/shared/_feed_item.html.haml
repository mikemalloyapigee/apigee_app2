%li#item{:id => "#{feed_item.id}"}
  %span.user
    =link_to feed_item.user.name, feed_item.user
  %span.content
    = feed_item.content
  %span.timestamp
    Posted
    = time_ago_in_words(feed_item.created_at)
    ago.